# Merged from the repo-root deployment.yaml at render time (the consuming
# Application sets helm.valueFiles: [$values/deployment.yaml]). Only adminEmails
# is used here; the other deployment.yaml keys are received and ignored.
adminEmails: []
